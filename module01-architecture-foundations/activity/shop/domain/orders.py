"""
TODO (Module 1 activity): move the business rule out of tangled_shop.py.

- Define InvalidOrderError here.
- Define a pure validate_order(lines) function that raises InvalidOrderError
  when `lines` is empty, and otherwise does nothing.
- Define order_total(lines) that sums unit_price * quantity across lines.

This module must NOT import anything from shop.infrastructure or shop.api —
that's the dependency-direction rule from the README.
"""

# TODO: implement InvalidOrderError, validate_order, order_total
