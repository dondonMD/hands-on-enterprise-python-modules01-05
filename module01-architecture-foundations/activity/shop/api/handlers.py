"""
TODO (Module 1 activity): this is the thin orchestration layer.

Define:
  - place_order(repo, order_id, customer_id, lines) -> str
      validates via shop.domain.orders, computes the total, saves via repo,
      and returns the same message tangled_shop.place_order used to return.
  - get_order_summary(repo, order_id) -> str
      same output format as tangled_shop.get_order_summary.
"""

# TODO: implement place_order, get_order_summary using shop.domain and the repo
